"use strict"; // this will treat all JS code as a new version

// alert(3 + 3); // this will give error in nodeJs because this only works in browser console

console.log("Hello"); console.log("World") //this is not a good practice

// refer mdn and ECMA 262 script docs link: {https://262.ecma-international.org/14.0/index.html?_gl=1*1gve636*_ga*NTY3NzMzNDU1LjE3Mzc0NjQxODM.*_ga_TDCK4DWEPP*MTczODkyMzM5Ny4zLjEuMTczODkyMzQ2Mi4wLjAuMA..}

let myName = "ABHINEET";
let age = 18;
let isLoggedIn = true;
let serverReq = null;
// number => 2^53 something is the limit
// bigint handles very large number
// string => "" / ''
// boolean => true / false
// undefined => this is a datatype which is given to the variables which are not initialized yet
// null => this is standlone value which is given to the variables of which we either
// don't have data or we don't want to initialize it now

// typeof keyword
console.log(typeof(vari));
console.log(typeof age); // output will be number
console.log(typeof 18); // same output because it gives datatypes of both variables and values

console.log(typeof isLoggedIn); // boolean

console.log(typeof myName); // string

console.log(typeof undefined); // undefined

console.log(typeof null); // object => this is because it is a 
                        // standalone value and is given to variables so it is not a datatype
                        // **this is my opinion**





